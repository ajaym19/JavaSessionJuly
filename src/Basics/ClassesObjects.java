package Basics;

public class ClassesObjects {

	/*
	 * Objects:
	 * anything which you see is an object
	 * objects can have:
	 * 1. Properties/Attributes: we define by variables
	 * 2. Behavior/Functions: by methods
	 * 
	 * Pen:
	 * 1. Prop: color, length, width, logo, brand, grip, 
	 * 2. Beha: write,
	 * 
	 * People:
	 * 1. Prop: name, age, address, color, nationality, id
	 * 2. Beha: walking, eating, running, sleeping
	 * 
	 * Constructors:
	 * initialize the variables
	 * when you create an object, call is made to constructor
	 * 2 types:
	 * Default
	 * Parametrized
	 * 
	 * 
	 * People as a class
	 * properties
	 * behavior
	 * 
	 * Sabya:
	 * 2 kids
	 * Raghav
	 * Achyut
	 * 
	 * Ajay:
	 * 2 kids:
	 * Raghav
	 * 
	 * 
	 * 
	 * Class:
	 * Animal Kingdom
	 * Human classification
	 * class of teachers
	 * class of students
	 * 
	 * Naming Conventions:
	 * package: com.facebook.login
	 * class: EmployeeDetails
	 * variables: camelCase, phoneNumber, ageMale
	 * methods: camelcase, getId, getName, getEmpDetails
	 * 
	 * 
	 */
}
